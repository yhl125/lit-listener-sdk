import { packagesCircuitBase } from './packages/circuit-base';

describe('packagesCircuitBase', () => {
  it('should work', () => {
    expect(packagesCircuitBase()).toEqual('packages/circuit-base');
  });
});
